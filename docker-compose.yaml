---
version: '3'
services:
  django:
    build:
      context: .
    command: gunicorn -b 0.0.0.0:8000 --access-logfile - tamedrift.wsgi
    environment:
      - DEBUG=True
      - SECRET_KEY=mysecretkey
      - DATABASE_URL=/Users/martin/PycharmProjects/tamedrift/backend/db.sqlite3
    ports:
      - "8000:8000"
    depends_on:
      - redis
  redis:
    image: "redis:latest"
